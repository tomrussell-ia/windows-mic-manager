<Window x:Class="MicrophoneManager.Views.FlyoutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:MicrophoneManager.Views"
        Title="Microphone Selection"
        Width="340" Height="480"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True"
    ResizeMode="NoResize"
    PreviewMouseLeftButtonDown="Window_PreviewMouseLeftButtonDown">

    <Border Background="#2D2D2D"
            CornerRadius="8"
            BorderBrush="#404040"
            BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" ShadowDepth="2" Opacity="0.5"/>
        </Border.Effect>

        <local:MicrophoneFlyout x:Name="Flyout" IsDockedMode="True"/>
    </Border>
</Window>
