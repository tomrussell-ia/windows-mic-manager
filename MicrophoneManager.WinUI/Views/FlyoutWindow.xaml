<Window
    x:Class="MicrophoneManager.WinUI.Views.FlyoutWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MicrophoneManager.WinUI.Views"
    Title="Microphone Selection">

    <Window.SystemBackdrop>
        <MicaBackdrop Kind="Base"/>
    </Window.SystemBackdrop>

    <Grid Background="{ThemeResource LayerFillColorDefaultBrush}"
          CornerRadius="8"
          Padding="16"
          BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
          BorderThickness="1">

        <Grid.Shadow>
            <ThemeShadow />
        </Grid.Shadow>

        <!-- Stage A: Placeholder content -->
        <StackPanel Spacing="12" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="🎤" FontSize="48" HorizontalAlignment="Center"/>
            <TextBlock Text="Microphone Manager" FontSize="20" FontWeight="SemiBold" HorizontalAlignment="Center"/>
            <TextBlock Text="Stage A: Skeleton App" FontSize="14" Foreground="{StaticResource AccentBrush}" HorizontalAlignment="Center"/>
            <TextBlock Text="Press ESC to close" FontSize="12" Foreground="#999999" HorizontalAlignment="Center" Margin="0,8,0,0"/>
        </StackPanel>
    </Grid>
</Window>
